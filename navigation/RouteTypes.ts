export enum MealsRoutes {
  CATEGORIES = 'Categories',
  CATEGORY_MEALS = 'CategoryMeals',
  MEAL_DETAIL = 'MealDetail',
}

export enum MealsFavs {
  MEALS = 'Meals',
  FAVORITES = 'Favorites',
}

export enum FavoriteMealsRoutes {
  FAVORITES = 'Favorites',
  MEAL_DETAIL = 'MealDetail',
}

export enum MainRoutes {
  MEALSFAVS = 'MealsFavs',
  FILTERS = 'Filters',
}

export enum FiltersRoutes {
  FILTERS = 'Filters',
}
